"use client";

import { useEffect, useMemo, useRef, useState } from "react";

const COOKIE = "echoes_beneath_state";
const LOOPS = "echoes_beneath_loops";
const WORLD_FALLBACK = { schemaVersion: 3, releasedDepth: 7, final: false, layerEvents: [
  { depth: 1, hourKey: "seed-01", motif: "螢幕邊緣浮出一圈霧", distortion: "倒影比你的動作快了半秒", whisper: "你選的是我，不是門。" },
  { depth: 2, hourKey: "seed-02", motif: "門把留下從房內握住的指紋", distortion: "走廊感應燈朝房間依序亮起", whisper: "不要把出口和安全混在一起。" },
  { depth: 3, hourKey: "seed-03", motif: "牆內傳來規律的三次敲擊", distortion: "敲擊重播了你的選擇", whisper: "另一個你已經走完這裡。" },
  { depth: 4, hourKey: "seed-04", motif: "所有鐘面同時少了一分鐘", distortion: "聲音開始比來源更早抵達", whisper: "相容的路會在下一個轉角交會。" },
  { depth: 5, hourKey: "seed-05", motif: "空氣出現濕冷的金屬味", distortion: "每個出口後都有同一雙鞋的摩擦聲", whisper: "它只能跟著你真正走過的地方。" },
  { depth: 6, hourKey: "seed-06", motif: "一塊陰影拒絕跟隨光線", distortion: "未選的動作出現在另一個表面", whisper: "別讓它冒充已經發生的事。" },
  { depth: 7, hourKey: "seed-07", motif: "某個門把比室溫低了十幾度", distortion: "異常晚一步留下能被追查的痕跡", whisper: "出口不一定是安全。" }
] };

const PLACES = {
  desk: { label: "房間的書桌前", short: "書桌", scene: "screen", anchors: ["關機螢幕", "桌面倒影", "椅背後方"], exits: ["door", "wall"] },
  door: { label: "房門內側", short: "房門", scene: "door", anchors: ["門縫下的光", "冰冷門把", "窺視孔"], exits: ["desk", "hall"] },
  wall: { label: "床頭旁的牆面", short: "牆邊", scene: "wall", anchors: ["牆內管線", "鬆動插座", "牆紙接縫"], exits: ["desk", "utility"] },
  hall: { label: "住處外的狹長走廊", short: "走廊", scene: "hall", anchors: ["感應燈", "沒有號碼的鄰門", "監視器"], exits: ["door", "stairs", "elevator"] },
  stairs: { label: "逃生梯轉角", short: "樓梯間", scene: "stairs", anchors: ["向下扶手", "半層平台", "防火門玻璃"], exits: ["hall", "lobby", "roof"] },
  elevator: { label: "電梯前的小廳", short: "電梯廳", scene: "elevator", anchors: ["永遠差一層的樓層燈", "不同步的電梯門", "緊急按鈕"], exits: ["hall", "laundry", "lobby"] },
  laundry: { label: "共用洗衣間", short: "洗衣間", scene: "laundry", anchors: ["未插電的洗衣機", "排水孔", "曬衣桿末端"], exits: ["elevator", "utility"] },
  utility: { label: "牆後維修空間", short: "維修夾層", scene: "pipes", anchors: ["溫熱水管", "纏結電線", "狹窄孔洞"], exits: ["wall", "laundry", "stairs"] },
  lobby: { label: "一樓無人的大廳", short: "一樓大廳", scene: "lobby", anchors: ["反鎖玻璃門", "住戶信箱", "停止轉動的監視器"], exits: ["stairs", "elevator", "street"] },
  roof: { label: "沒有其他出口的屋頂", short: "屋頂", scene: "roof", anchors: ["水塔背面", "唯一熄滅的窗", "剪斷的避雷線"], exits: ["stairs"] },
  street: { label: "大樓門口的騎樓", short: "騎樓", scene: "street", anchors: ["無車路口", "映不出你的玻璃", "閃爍招牌"], exits: ["lobby"] }
};

const THREATS = {
  reflection: { label: "倒影先行", entity: "比你早一拍的倒影", lines: ["倒影維持著你幾秒後才會做出的動作。", "反光表面都映出同一個不可能角度。", "倒影先把食指貼在嘴前。"] },
  threshold: { label: "門檻來客", entity: "始終站在另一側的人", lines: ["重量停在界線另一側，沒有影子越過來。", "同一個人似乎站在每個出口後方。", "你靠近一個出口，另一處便傳來敲擊。"] },
  voice: { label: "牆後回聲", entity: "借用熟悉聲音的回聲", lines: ["牆後聲音重複你的選擇，主詞卻換成自己。", "聲音模仿你還未完整形成的下一句。", "管線呼吸能說出你停下的位置。"] },
  duplicate: { label: "未選之人", entity: "做了另一個選擇的你", lines: ["另一個你正在完成未被選的動作。", "相同腳步與你同速，方向完全相反。", "輪廓帶著你本來可能取得的物品。"] },
  erased: { label: "被刪掉的位置", entity: "沒有被記錄的第四個選項", lines: ["三個選項間多出向外鼓起的空白。", "附近物件替不存在的人空出位置。", "你記得第四個選項，卻想不起內容。"] }
};

const THREAT_KEYS = Object.keys(THREATS);
const STATS = { nerve: ["◆", "鎮定"], insight: ["◇", "警覺"], empathy: ["◌", "共感"] };
const ROOT_CONTEXT = { place: "desk", from: null, threat: "reflection", relation: "unknown", pressure: 0, carry: "網站剛剛自行亮起。" };
const freshState = () => ({ version: 3, node: "root", depth: 0, context: { ...ROOT_CONTEXT }, history: [], stats: { nerve: 0, insight: 0, empathy: 0 }, flags: [], items: [], result: null, pending: null, ending: null });
const h = (text) => { let value = 2166136261; for (const char of String(text)) { value ^= char.charCodeAt(0); value = Math.imul(value, 16777619); } return Math.abs(value >>> 0); };
const pick = (list, seed, offset = 0) => list[(seed + offset * 7919) % list.length];
const clamp = (value, min, max) => Math.max(min, Math.min(max, value));
const place = (context) => PLACES[context?.place] || PLACES.desk;
const threat = (context) => THREATS[context?.threat] || THREATS.reflection;
const nextThreat = (current, offset) => THREAT_KEYS[(THREAT_KEYS.indexOf(current) + offset + THREAT_KEYS.length) % THREAT_KEYS.length];
const route = (stats) => { const entries = Object.entries(stats).sort((a, b) => b[1] - a[1]); return entries[0][1] === entries[1][1] ? "balanced" : entries[0][0]; };
const routeName = (key) => ({ nerve: "不回頭的人", insight: "看見規則的人", empathy: "聽見回聲的人", balanced: "尚未被預測的人" }[key]);
const allowed = (from, to) => from && to && (from.place === to.place || PLACES[from.place]?.exits.includes(to.place));
const unlocked = (depth, world) => world.final || depth <= Number(world.releasedDepth || 0);
const branchCount = (depth) => depth < 13 ? (3 ** depth).toLocaleString("zh-TW") : `約 10^${(depth * Math.log10(3)).toFixed(1)}`;
const timeText = (date = new Date()) => new Intl.DateTimeFormat("zh-TW", { hour: "2-digit", minute: "2-digit", hour12: false }).format(date);
const personalized = (text, date) => String(text || "").replaceAll("{{time}}", timeText(date));

function rootNode() {
  return { id: "root", title: "第一刻：房間多了一個呼吸", subtitle: "三個選擇會立刻分成三條不同路線。", scene: "screen", accent: "red", context: ROOT_CONTEXT,
    intro: "現在是 {{time}}。你坐在書桌前，網站自行亮起。喇叭沒有播放聲音，但你仍聽見另一段呼吸，像隔著螢幕、門板或牆面其中之一。當你屏住氣，它多呼吸了一次。",
    reactions: { nerve: "你先確認手邊能當武器的東西，呼吸因此停頓。", insight: "呼吸會在游標停住時變近，它正在利用注意力定位。", empathy: "呼吸並不平穩，像有人努力忍住哭聲。", balanced: "你還沒有固定反應，所以它無法預測你。" },
    choices: [
      { id: "reflection", label: "利用關機螢幕的倒影確認身後", icon: "◉", result: "你沒有回頭。倒影中的你卻已站起來，把臉貼近螢幕內側。你仍坐在原位。", delta: { nerve: 1, insight: 2, empathy: 0 }, flag: "root_reflection", next: "branch-reflection", context: { place: "desk", from: "desk", threat: "reflection", relation: "watching", pressure: 1, carry: "你留在書桌前，確認異常存在於反光表面。" } },
      { id: "threshold", label: "熄掉桌燈，退到房門內側確保出口", icon: "▯", result: "你退到房門內側，門縫外已有一雙沒有影子的腳，腳尖朝向房間。", delta: { nerve: 2, insight: 0, empathy: -1 }, flag: "root_threshold", next: "branch-threshold", context: { place: "door", from: "desk", threat: "threshold", relation: "hostile", pressure: 2, carry: "你離開書桌站到房門內側；門外有一雙沒有影子的腳。" } },
      { id: "voice", label: "走到床頭牆邊，把耳朵貼上牆面", icon: "◌", result: "呼吸立刻變清楚，接著用你的聲音說：「終於選到我了。」", delta: { nerve: 0, insight: 1, empathy: 2 }, item: "牆後的第一句話", next: "branch-voice", context: { place: "wall", from: "desk", threat: "voice", relation: "curious", pressure: 1, carry: "你離開書桌來到床頭牆邊；牆後聲音已回答。" } }
    ] };
}

function dynamicNode(state, world) {
  const ctx = state.context, here = place(ctx), danger = threat(ctx);
  const event = world.layerEvents?.find((item) => item.depth === state.depth) || { hourKey: `fallback-${state.depth}`, motif: "空氣出現淡淡金屬味", distortion: "聲音比動作晚半秒", whisper: "別把下一步想得太清楚。" };
  const seed = h(`${state.node}:${state.depth}:${event.hourKey}`), anchor = pick(here.anchors, seed), moveId = pick(here.exits, seed, 1), otherId = pick(here.exits.filter((id) => id !== moveId).length ? here.exits.filter((id) => id !== moveId) : here.exits, seed, 2), nextDepth = state.depth + 1, weave = nextDepth % 4 === 0;
  const inspectThreat = nextThreat(ctx.threat, 1 + seed % 2), moveThreat = nextThreat(ctx.threat, 2), talkThreat = nextThreat(ctx.threat, 3 + seed % 2), pressure = clamp(Number(ctx.pressure || 0), 0, 9);
  const inspectCtx = { place: ctx.place, from: ctx.place, threat: inspectThreat, relation: ctx.relation === "hostile" ? "measuring" : "watching", pressure: clamp(pressure + 1, 0, 9), carry: `你沒有離開${here.short}，並從${anchor}確認異常改變形態。` };
  const moveCtx = { place: moveId, from: ctx.place, threat: moveThreat, relation: pressure >= 5 ? "pursuing" : ctx.relation, pressure: clamp(pressure + 1, 0, 9), carry: `你沿明確出口抵達${PLACES[moveId].short}；異常跟了過來。` };
  const talkPlace = weave ? otherId : ctx.place, talkCtx = { place: talkPlace, from: ctx.place, threat: talkThreat, relation: ctx.relation === "hostile" ? "uncertain" : "recognized", pressure: clamp(pressure + (ctx.relation === "hostile" ? 0 : -1), 0, 9), carry: weave ? `你沿實際通道抵達${PLACES[talkPlace].short}；相容路線在此交會。` : `你留在${here.short}建立可驗證的回應規則。` };
  const key = (action, target, joined = false) => joined ? `weave-${h(`${target.place}:${target.threat}:${Math.floor(nextDepth / 4)}`).toString(36)}` : `node-${h(`${state.node}:${action}:${target.place}:${target.threat}:${nextDepth}`).toString(36)}`;
  return { id: state.node, title: `第 ${String(state.depth + 1).padStart(2, "0")} 刻：${anchor}沒有照原來的位置存在`, subtitle: `${event.motif}。這一幕只延續你真正走到的${here.short}。`, scene: here.scene, accent: ["red", "amber", "blue", "violet", "green"][seed % 5], context: ctx,
    intro: `${ctx.from && ctx.from !== ctx.place ? `你從${PLACES[ctx.from]?.short || "上一處"}來到${here.label}` : `你仍在${here.label}`}。${ctx.carry || ""} ${pick(danger.lines, seed, 3)} ${event.distortion}。${danger.entity}低聲說：「${event.whisper}」`,
    reactions: { nerve: `你沒有退後。${danger.entity}改變站位，避開正面視線。`, insight: `異常只能利用${here.short}存在的物件與出口，不能憑空換場景。`, empathy: `${danger.entity}正描述一條不同、卻能在${here.short}交會的路。`, balanced: `你的反應仍不固定，${danger.entity}無法決定封鎖哪個出口。` },
    choices: [
      { id: "inspect", label: `留在${here.short}，直接檢查${anchor}`, icon: "◎", result: `你沒有改變所在地。${event.motif}向內縮去，留下${THREATS[inspectThreat].label}的痕跡。`, delta: { nerve: 1, insight: 2, empathy: 0 }, flag: `inspect_${ctx.place}_${state.depth}`, next: key("inspect", inspectCtx), context: inspectCtx },
      { id: "move", label: `經由可見出口前往${PLACES[moveId].short}`, icon: "→", result: `你確認通道確實連向${PLACES[moveId].label}才離開。${THREATS[moveThreat].entity}跟得上你，卻不能改寫經過的路。`, delta: { nerve: 2, insight: 0, empathy: -1 }, flag: `move_${ctx.place}_${moveId}`, next: key("move", moveCtx), context: moveCtx },
      { id: "interact", label: weave ? `循回應前往${PLACES[otherId].short}確認交會` : `不離開${here.short}，要求可驗證的回答`, icon: "◇", result: weave ? `你沿真實通道抵達${PLACES[otherId].label}。另一條分支也指向同一處，故事只在場景與線索相容時交織。` : `${danger.entity}回答了只有目前場所能驗證的細節，證明這仍是同一條因果鏈。`, delta: { nerve: 0, insight: 1, empathy: 2 }, item: weave ? `交織座標 ${talkPlace}-${Math.floor(nextDepth / 4)}` : `可驗證回應 ${state.depth}`, next: key("interact", talkCtx, weave), context: talkCtx }
    ] };
}

function finalNode(state) {
  const here = place(state.context), danger = threat(state.context);
  return { id: `final-${state.node}`, final: true, title: `最終刻：${here.short}只剩下三種聲音`, subtitle: `你經過 ${state.history.length} 次選擇來到這裡。`, scene: here.scene, accent: "red", context: state.context,
    intro: `7 月 31 日最後一次更新後，${here.label}沒有恢復正常。${danger.entity}承認自己由每次未選的結果構成，只能沿真正走過的場所與線索跟到這裡。`, reactions: { nerve: "你能切斷入口，但再也無法確認被切斷的分支。", insight: "路線會分裂，只在因果相容時交織。", empathy: "每個威脅裡都混著未被選擇者留下姓名的請求。", balanced: "你仍保留不屬於任何預測的收尾。" },
    choices: [
      { id: "sever", label: `封死${here.short}入口並刪除路徑`, icon: "✕", ending: "severed", result: `你關閉與${danger.entity}相連的入口。——結局：只保留走到這裡的人。`, delta: { nerve: 3, insight: 0, empathy: -2 }, flag: "ending_severed" },
      { id: "map", label: "把節點畫成地圖並保留相容入口", icon: "⌘", ending: "mapped", result: "你建立索引，讓相容路線交會、不相容路線永遠分開。——結局：回聲拓撲圖。", delta: { nerve: 0, insight: 3, empathy: 0 }, item: "完整故事圖" },
      { id: "coexist", label: `讓${danger.entity}留下，但不能改寫事實`, icon: "◇", ending: "coexist", result: `你承認未選結果也曾可能發生，卻拒絕它們冒充目前路線。——結局：與未選之人保持距離。`, delta: { nerve: 0, insight: 0, empathy: 3 }, flag: "ending_coexist" }
    ] };
}

const nodeFor = (state, world) => state.node === "root" ? rootNode() : world.final && state.depth >= 8 ? finalNode(state) : dynamicNode(state, world);
const readCookie = (name) => { const row = document.cookie.split("; ").find((item) => item.startsWith(`${name}=`)); return row ? decodeURIComponent(row.split("=").slice(1).join("=")) : null; };
const writeCookie = (name, value) => { document.cookie = `${name}=${encodeURIComponent(value)}; expires=${new Date(Date.now() + 15552000000).toUTCString()}; path=/; SameSite=Lax${window.location.protocol === "https:" ? "; Secure" : ""}`; };
const encode = (value) => { try { return btoa(unescape(encodeURIComponent(JSON.stringify(value)))); } catch { return ""; } };
const decode = (value) => { try { return JSON.parse(decodeURIComponent(escape(atob(value)))); } catch { return null; } };
const normalize = (value) => value?.version === 3 && value.context && Number.isInteger(value.depth) ? { ...freshState(), ...value, stats: { ...freshState().stats, ...(value.stats || {}) }, history: Array.isArray(value.history) ? value.history.slice(-100) : [], flags: Array.isArray(value.flags) ? value.flags.slice(-100) : [], items: Array.isArray(value.items) ? value.items.slice(-60) : [] } : freshState();

function Scene({ node, clicks, inspect }) {
  const here = place(node.context), danger = node.context?.threat || "reflection", palette = { red: ["#ff4b55", "#3c080d"], amber: ["#ffb03b", "#3c2106"], blue: ["#63b3ff", "#071f3c"], violet: ["#b58cff", "#24103f"], green: ["#75e5a1", "#0b3420"] }[node.accent] || ["#ff4b55", "#3c080d"], seed = h(node.id);
  return <button className="art-button" onClick={inspect} aria-label="調查場景異常"><svg className="scene-art" viewBox="0 0 1000 600"><defs><linearGradient id={`b-${node.id}`} x2="1" y2="1"><stop stopColor="#020304"/><stop offset=".66" stopColor="#090b0f"/><stop offset="1" stopColor={palette[1]}/></linearGradient></defs><rect width="1000" height="600" fill={`url(#b-${node.id})`}/>{Array.from({ length: 26 }, (_, index) => <circle key={index} cx={seed * (index + 13) * 17 % 1000} cy={seed * (index + 7) * 29 % 600} r={index % 6 ? 1 : 2} fill="#fff" opacity=".08"/>)}<path d="M80 560L230 105H770L920 560Z" fill="#050608" stroke="#24292e" strokeWidth="4"/>{(here.scene === "screen" || here.scene === "lobby") && <g><rect x="300" y="140" width="400" height="280" fill="#020304" stroke="#3a4047" strokeWidth="9"/><rect x="325" y="165" width="350" height="230" fill={palette[1]}/></g>}{(here.scene === "door" || here.scene === "hall") && <g><rect x="390" y="125" width="220" height="370" fill="#070707" stroke={palette[0]} strokeWidth="5"/><path d="M420 497H580" stroke="#ffe6ad" strokeWidth="8"/></g>}{here.scene === "wall" && <path d="M430 210q70-80 140 0v230q-70 55-140 0z" fill="#020304" stroke={palette[0]} strokeWidth="4"/>}{here.scene === "stairs" && Array.from({ length: 8 }, (_, index) => <path key={index} d={`M${260 + index * 35} ${510 - index * 38}H${610 + index * 12}`} stroke="#3b4045" strokeWidth="13"/>)}{here.scene === "elevator" && <g><rect x="330" y="120" width="340" height="400" fill="#08090a" stroke="#4a4f55" strokeWidth="8"/><path d="M500 120V520" stroke="#34393e" strokeWidth="5"/></g>}{here.scene === "laundry" && <g><circle cx="410" cy="320" r="105" fill="#070809" stroke="#4a5056" strokeWidth="12"/><circle cx="590" cy="320" r="105" fill="#070809" stroke="#4a5056" strokeWidth="12"/></g>}{here.scene === "pipes" && <path d="M220 160H760M280 110V540M710 110V540" stroke="#464b50" strokeWidth="26"/>}{here.scene === "roof" && <path d="M80 450L250 300 410 390 590 260 760 390 920 280V560H80Z" fill="#030405"/>}{here.scene === "street" && <path d="M110 470H890M500 120V480" stroke="#373c41" strokeWidth="16"/>}{danger === "reflection" && <g><circle cx="465" cy="325" r="7" fill={palette[0]}/><circle cx="535" cy="325" r="7" fill={palette[0]}/></g>}{danger === "threshold" && <path d="M500 190C430 230 420 390 435 530H565C580 390 570 230 500 190Z" fill="#010203"/>}{danger === "voice" && <path d="M410 300Q500 220 590 300M390 350Q500 260 610 350" fill="none" stroke={palette[0]} strokeWidth="5"/>}{danger === "duplicate" && <g><path d="M445 520C445 370 468 250 500 230C532 250 555 370 555 520Z" fill="#010203"/><path d="M555 520C555 390 578 280 610 260C642 280 665 390 665 520Z" fill="#010203"/></g>}{danger === "erased" && <rect x="420" y="180" width="160" height="300" fill="#000" stroke={palette[0]} strokeDasharray="12 15" strokeWidth="4"/>}<ellipse cx="855" cy="92" rx={clicks >= 6 ? 70 : 42} ry={clicks >= 6 ? 28 : 10} fill="#020304" stroke={palette[0]} strokeWidth="3"/><circle cx="855" cy="92" r={clicks >= 6 ? 15 : 6} fill={palette[0]}/><text x="32" y="48" fill="#fff" opacity=".38" fontSize="17">ECHOES // {node.id.toUpperCase()}</text></svg><span className="art-hint">畫面右上角似乎有東西</span></button>;
}

export default function GameRuntime() {
  const [world, setWorld] = useState(WORLD_FALLBACK), [state, setState] = useState(freshState()), [ready, setReady] = useState(false), [now, setNow] = useState(new Date()), [loops, setLoops] = useState(0), [restartOpen, setRestartOpen] = useState(false), [archiveOpen, setArchiveOpen] = useState(false), [toast, setToast] = useState(""), [clicks, setClicks] = useState(0), [sound, setSound] = useState(false);
  const audio = useRef(null);
  useEffect(() => { setState(normalize(decode(readCookie(COOKIE) || ""))); setLoops(Number(readCookie(LOOPS) || 0)); fetch(`https://raw.githubusercontent.com/2lll5/Echoes-Beneath/main/public/story.generated.json?t=${Date.now()}`, { cache: "no-store" }).then((response) => response.ok ? response.json() : Promise.reject()).then((data) => data?.schemaVersion === 3 && setWorld(data)).catch(() => {}).finally(() => setReady(true)); }, []);
  useEffect(() => { if (ready) writeCookie(COOKIE, encode(state)); }, [state, ready]);
  useEffect(() => { const timer = setInterval(() => setNow(new Date()), 15000); return () => clearInterval(timer); }, []);
  useEffect(() => { let idle; const active = () => { clearTimeout(idle); idle = setTimeout(() => setToast("你停得太久。這條路線裡只有你沒有繼續移動。"), 23000); }; const visibility = () => { if (document.hidden) document.title = "你離開了。這條分支沒有。"; else { document.title = "Echoes Beneath｜底下的回聲"; setToast("所在地沒變，但某個物件的位置不同。" ); } }; ["mousemove", "keydown", "touchstart", "scroll"].forEach((event) => addEventListener(event, active, { passive: true })); document.addEventListener("visibilitychange", visibility); active(); return () => { clearTimeout(idle); ["mousemove", "keydown", "touchstart", "scroll"].forEach((event) => removeEventListener(event, active)); document.removeEventListener("visibilitychange", visibility); }; }, []);
  useEffect(() => { if (!toast) return; const timer = setTimeout(() => setToast(""), 4600); return () => clearTimeout(timer); }, [toast]);
  useEffect(() => () => audio.current?.close(), []);
  const node = useMemo(() => nodeFor(state, world), [state, world]), here = place(state.context), danger = threat(state.context), pathRoute = route(state.stats), canAdvance = state.pending ? unlocked(state.pending.depth, world) : false, progress = Math.min(100, state.depth / Math.max(Number(world.releasedDepth || 1), 1) * 100);
  const choose = (choice) => { if (state.result || state.ending) return; if (!node.final && !allowed(state.context, choice.context)) return setToast("連貫性檢查阻止了一個不可能的場景跳轉。"); const stats = { ...state.stats }; Object.entries(choice.delta || {}).forEach(([key, value]) => { stats[key] = (stats[key] || 0) + value; }); const result = personalized(choice.result, now), nextPlace = choice.context ? place(choice.context) : here, entry = { node: state.node, depth: state.depth, location: here.short, choice: choice.label, nextLocation: nextPlace.short }; setState((old) => ({ ...old, stats, flags: choice.flag ? [...new Set([...old.flags, choice.flag])].slice(-100) : old.flags, items: choice.item ? [...new Set([...old.items, choice.item])].slice(-60) : old.items, history: [...old.history, entry].slice(-100), result, pending: node.final ? null : { node: choice.next, context: choice.context, depth: old.depth + 1 }, ending: node.final ? choice.ending : old.ending })); };
  const advance = () => { if (!state.pending || !canAdvance) return; setState((old) => ({ ...old, node: old.pending.node, context: old.pending.context, depth: old.pending.depth, result: null, pending: null })); setClicks(0); };
  const reset = () => { const value = loops + 1; writeCookie(LOOPS, String(value)); setLoops(value); setState(freshState()); setClicks(0); setRestartOpen(false); setToast(value === 3 ? "第三次重來後，根節點記得你曾選過另外兩條路。" : "分支樹已重置，但重啟次數仍被記住。" ); };
  const inspect = () => { const value = clicks + 1; setClicks(value); if (value === 3) setToast(`它剛才看向${here.short}的出口。`); if (value === 6) { setToast("彩蛋：獲得拓撲之眼。" ); setState((old) => ({ ...old, items: [...new Set([...old.items, "拓撲之眼"])] })); } };
  const toggleSound = async () => { if (sound) { await audio.current?.close(); audio.current = null; setSound(false); return; } const Context = window.AudioContext || window.webkitAudioContext; if (!Context) return; const context = new Context(), master = context.createGain(); master.gain.value = .025; master.connect(context.destination); [43, 57, 86].forEach((frequency, index) => { const oscillator = context.createOscillator(), gain = context.createGain(); oscillator.type = index === 2 ? "sine" : "triangle"; oscillator.frequency.value = frequency; gain.gain.value = index === 2 ? .16 : .32; oscillator.connect(gain).connect(master); oscillator.start(); }); audio.current = context; setSound(true); setToast("環境音已開啟。有些腳步只會在目前所在地出現。" ); };
  if (!ready) return <main className="loading-screen"><div className="loader"/><p>正在確認故事連線……</p></main>;
  return <main className="game-shell" onPointerMove={(event) => { event.currentTarget.style.setProperty("--mx", ((event.clientX / innerWidth - .5) * 2).toFixed(3)); event.currentTarget.style.setProperty("--my", ((event.clientY / innerHeight - .5) * 2).toFixed(3)); }}><div className="noise"/><div className="watcher"><i/><i/></div><header className="topbar"><div><span className="eyebrow">BRANCHING HORROR GRAPH / COOKIE MEMORY</span><h1>Echoes Beneath <small>底下的回聲</small></h1></div><div className="header-actions"><button className="ghost-button" onClick={toggleSound}>{sound ? "關閉環境音" : "開啟環境音"}</button><button className="ghost-button" onClick={() => setArchiveOpen(true)}>分支路徑</button><button className="danger-button" onClick={() => setRestartOpen(true)}>重新開始</button></div></header><section className="presence-strip"><span className="live-dot"/><p>本地時間 <strong>{timeText(now)}</strong> · 目前所在地：<strong>{here.short}</strong></p><span>已釋出深度 {world.releasedDepth} · 7/31 收尾</span></section><section className="progress-wrap"><div className="progress-meta"><span>故事深度 {state.depth} / {world.releasedDepth} · 理論路徑 {branchCount(world.releasedDepth)}</span><span>{routeName(pathRoute)} · {danger.label}</span></div><div className="progress-track"><span style={{ width: `${progress}%` }}/></div></section>{state.ending ? <section className="ending-card"><span className="eyebrow">THE END REMEMBERS THE WHOLE PATH</span><h2>{here.short}終於安靜下來</h2><p>{state.result}</p><p className="ending-code">結局：{state.ending} · 經過 {state.history.length} 個節點</p><button className="danger-button solid" onClick={() => setRestartOpen(true)}>回到根節點</button></section> : <div className="game-grid"><section className="story-panel"><Scene node={node} clicks={clicks} inspect={inspect}/><article className="story-card"><div className="chapter-line"><span>{node.final ? "最終節點" : `故事節點 ${node.id}`}</span><span>{here.short} / {danger.label}</span></div><h2>{node.title}</h2><p className="subtitle">{node.subtitle}</p><p className="story-copy">{personalized(node.intro, now)}</p><div className="route-note"><span>這條路線的反應</span><p>{node.reactions[pathRoute] || node.reactions.balanced}</p></div>{!state.result ? <div className="choices">{node.choices.map((choice, index) => <button className="choice-card" key={`${node.id}-${choice.id}`} onClick={() => choose(choice)}><span className="choice-index">0{index + 1}</span><span className="choice-icon">{choice.icon}</span><span className="choice-label">{choice.label}</span><span className="choice-arrow">→</span></button>)}</div> : <div className="result-card"><span className="eyebrow">CHOICE STORED WITH ITS NEXT NODE</span><h3>所在地與威脅已改變</h3><p>{state.result}</p>{!node.final && (canAdvance ? <button className="primary-button" onClick={advance}>進入「{place(state.pending.context).short}」的下一節點 →</button> : <div className="locked-branch"><strong>這條分支尚未釋出下一層</strong><p>下一個所在地已保存在 Cookie，不會硬接回其他故事。</p><button className="ghost-button" onClick={() => window.location.reload()}>檢查整點更新</button></div>)}</div>}</article></section><aside className="status-panel"><div className="status-card identity-card"><span className="eyebrow">SUBJECT</span><div className="avatar">Z<span>{loops || ""}</span></div><h3>{routeName(pathRoute)}</h3><p>走過 {state.history.length} 個節點 · 重啟 {loops} 次</p></div><div className="status-card"><div className="status-heading"><h3>連貫性狀態</h3><span>決定下一幕</span></div><div className="continuity-list"><p><strong>所在地</strong><span>{here.label}</span></p><p><strong>危險來源</strong><span>{danger.entity}</span></p><p><strong>關係</strong><span>{state.context.relation}</span></p><p><strong>壓力</strong><span>{state.context.pressure} / 9</span></p></div></div><div className="status-card"><div className="status-heading"><h3>心理輪廓</h3></div>{Object.entries(STATS).map(([key, meta]) => <div className="stat-row" key={key}><span>{meta[0]} {meta[1]}</span><div className="stat-bar"><i style={{ width: `${Math.min(100, 18 + Math.max(0, state.stats[key]) * 7)}%` }}/></div><strong>{state.stats[key]}</strong></div>)}</div><div className="status-card"><div className="status-heading"><h3>線索</h3><span>{state.items.length}</span></div><div className="item-list">{state.items.length ? state.items.slice(-7).map((item) => <span key={item}>{item}</span>) : <p>尚無線索。</p>}</div></div></aside></div>}<footer><span>Browser Cookie · Schema v3</span><span>分支因子 3 · 已釋出深度 {world.releasedDepth}</span></footer>{restartOpen && <div className="modal-backdrop" onClick={() => setRestartOpen(false)}><div className="modal" onClick={(event) => event.stopPropagation()}><h2>回到根節點？</h2><p>目前路徑、所在地、數值與線索會清空；重啟次數保留。</p><div className="modal-actions"><button className="ghost-button" onClick={() => setRestartOpen(false)}>取消</button><button className="danger-button solid" onClick={reset}>清除路徑</button></div></div></div>}{archiveOpen && <div className="modal-backdrop" onClick={() => setArchiveOpen(false)}><div className="modal archive-modal" onClick={(event) => event.stopPropagation()}><h2>你真正走過的路</h2><div className="archive-grid"><div><strong>{state.history.length}</strong><span>節點</span></div><div><strong>{state.flags.length}</strong><span>事件</span></div><div><strong>{loops}</strong><span>重啟</span></div></div><div className="path-list">{state.history.length ? state.history.slice(-14).map((entry, index) => <div key={`${entry.node}-${index}`}><span>{String(entry.depth + 1).padStart(2, "0")}</span><p><strong>{entry.location}</strong>－{entry.choice}<small>下一站：{entry.nextLocation}</small></p></div>) : <p>尚未離開根節點。</p>}</div><button className="primary-button" onClick={() => setArchiveOpen(false)}>回到目前節點</button></div></div>}{toast && <div className="toast">{toast}</div>}</main>;
}
